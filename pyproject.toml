[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "snapdock-guppy"
version = "5.0.0"
description = "SnapDock — local-first desktop Markdown application"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"

authors = [
  { name = "ZFordDev", email = "zforddev@gmail.com" }
]
maintainers = [
  { name = "ZFordDev", email = "zforddev@gmail.com" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

dependencies = [
    "PySide6",
    "markdown",
    "pymdown-extensions",
]

[project.optional-dependencies]
dev = ["pytest", "black", "ruff", "pyinstaller"]

[tool.setuptools]
packages = ["snapdock"]

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "SIM", "BLE"]
# N802: PySide6 requires camelCase override names (insertFromMimeData, keyPressEvent).
# BLE001: UI file/rendering code intentionally surfaces errors instead of crashing.
ignore = ["N802", "BLE001"]

[project.scripts]
snapdock-guppy = "snapdock.main:main"
